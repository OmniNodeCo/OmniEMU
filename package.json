{
  "name": "omniemu",
  "version": "1.0.0",
  "private": true,
  "description": "A polished cross-platform emulator dashboard prototype",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  }
}
